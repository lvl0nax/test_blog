  install:
    * redis for sidekiq
    * postgres
    * imagemagick
  
  terminal commands:
  bundle install
  rake db:create
  rake db:migrate
  rails s

  active_admin(http://localhost:3000/admin) - admin panel for creation articles
  (User: admin@example.com)
  (Password: password)
